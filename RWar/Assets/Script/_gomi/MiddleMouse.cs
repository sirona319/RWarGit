using System.Collections;
using UnityEngine;


public class MiddleMouse : MonoBehaviour
{
    //const int MIDDLE = 2;
    //// Use this for initialization
    //void Start()
    //{

    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (!Input.GetMouseButton(MIDDLE)) return;

    //    Debug.Log("MiddleMouse");

    //    var movePoint = MyLib.DebugRayViewCameraPosXZ();

    //    Camera.main.transform.position = new Vector3(movePoint.x, Camera.main.transform.position.y, movePoint.z);
    //}
}
